Discord bot (1.7.3) that forwards message received in a channel from one server to a channel in another server.

You will need to set up a config.json folder with your discord token:
{
  "credentials": {
    "Token": "your-token"
  }
}
